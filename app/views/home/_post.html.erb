<tr>
	<td><h2><%= link_to post.titulo, post_path(post.id)%></h2></td>
</tr>	
<tr>
	<td><%= post.corpo %></td>
</tr>	
<tr>
	<% unless post.user.profile.nil? %>
		<td>Autor: <%= link_to "#{post.user.profile.nome}", profile_path(post.user.profile.id) %> <%=" - Data: #{post.created_at}" %></td>
	<% else %>
		<td><%= "Autor: Não Identificado - Data: #{post.created_at}" %></td>
	<% end %>
</tr>